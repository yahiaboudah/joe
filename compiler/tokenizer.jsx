

//@include "../src/xto+.jsx"

;eval(MODULE.re("$.global", "tokenizer", "tokenize"))

    [STATIC]
    ({
        __name__: "main",

        tokenize: function(str)
        {
            var i=-1;
            while(++i<str.length){
                if(true){}
            }
        }
    })
